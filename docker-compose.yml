services:
  redis:
    image: redis:8
    container_name: company-leaves-redis
    ports:
      - "6379:6379"
    command: >
      redis-server
      --save ""
      --appendonly no
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5
